{
  "name": "notebackend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "build:ui": "rm -rf dist && cd ../fullstackopen_materiaaliharkat/osa2/noteapp_frontend && npm run build && cp -r dist ~/fso_noteapp_production_build",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push -u origin main",
    "lint": "eslint ."
  },
  "author": "Roope Salminen",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "mongoose": "^8.6.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "@stylistic/eslint-plugin-js": "^2.8.0",
    "cross-env": "^7.0.3",
    "eslint": "^9.10.0",
    "globals": "^15.9.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.0.0"
  }
}
